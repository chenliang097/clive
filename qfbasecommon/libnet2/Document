新的网络请求使用
1.需要些Retrofit标准的Interface接口

2.new NetWorks
第一个参数 lifecycleowner 比如activity或者fragment(两者都是support包里的)
第二个参数回调接口里面要配置需要解析的model

3.在create方法里通过retrofit参数创建要调用的接口

4.最后要调用start方法开始请求

5.gson解析的model
必须要继承自BaseModel 里面默认解析了errNo和errMsg
model里的解析变量都需要初始化
列如
@SerializedName("avatar")
public String avatar = "";

